featureserver {
  db {
    url="jdbc:postgresql://localhost:9432/fserver"
    user="fserver"
    password=""
    driver="org.postgresql.Driver"
    connections = {
      poolSize = 10
    }
  }
  server {
    host="0.0.0.0"
    port=8080
  }
}
